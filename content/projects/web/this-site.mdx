---
title: This Site
updated: 2026-04-23
---

Designed based on Arch/i3 config - available with scripted install [here](/projects/systems/dotfiles).

## Stack

- Vite 6, React 19, TypeScript
- Tailwind CSS v4
- react-router-dom v7

## Content pipeline

- MDX via `@mdx-js/rollup`
- `remark-frontmatter` + `remark-mdx-frontmatter` for post metadata
- `remark-math` + `rehype-katex` for LaTeX
- `@shikijs/rehype` (`vitesse-black` theme) for syntax highlighting
- Posts auto-discovered via `import.meta.glob` — folder tree `content/<kind>/<category>/<slug>.mdx` is the URL tree

## Layout

- Single-tree site map (press `s`) and command menu (`h`)
- Colour cycling via `c` — six accent hues, persisted in localStorage
- Responsive: top nav on desktop, fixed bottom bar on mobile

## Extended functionality
- Try `h`

## Deploy

- Netlify, auto-build on push to `main`
- `_redirects` for SPA routing

---

## Changelog

### 04/30/26
- Removed mobile nav bar, bar now stays at top of screen, fixes inconsistent dynamic address bar behaviour on mobile browsers
- Added clickable submenu "hints" to main page so mobile users can access entire site
- Added clickable help menu for mobile users
- Unselected animation behaviour: The "err" animation upon attemting to enter an empty dir made conditional upon dir being selected
- Removed stray react png
- Glitch burst as top overlay
